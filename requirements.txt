# ES TimeTrack - Python dependencies (target: Python 3.11+)
# Pinned to known-good releases. Install with:  pip install -r requirements.txt

fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
pydantic-settings==2.7.1
Jinja2==3.1.5
python-multipart==0.0.20      # parsing HTML form POSTs
argon2-cffi==23.1.0           # password hashing
itsdangerous==2.2.0           # signs the session cookie

# Phase 5/6 (declared now so the venv is complete; unused until then):
APScheduler==3.11.0           # email reminder scheduler
openpyxl==3.1.5               # .xlsx export
