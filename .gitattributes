# Normalize line endings: store text as LF in the repo, check out native.
* text=auto

# Keep PowerShell scripts as CRLF on checkout (Windows-native).
*.ps1 text eol=crlf

# Treat vendored minified assets and the SVG/icon as-is.
app/static/js/*.min.js -text
